
import * as THREE from "three";

const SectionMesh = ({ mesh }: { mesh: THREE.Mesh}) => {
    return(
        <>
            {
                mesh
            }
        </>
    )
}

export default SectionMesh;